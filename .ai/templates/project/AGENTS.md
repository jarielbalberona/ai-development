# Project Agent Contract

This template is agent-agnostic.

Prefer the parent `.ai/core/` rules when accessible.
If parent `.ai/` is not accessible, fall back to `.ai-local/core-snapshot.md`.

Read:

- `.ai-project.md`
- `project-canon/README.md`
- Relevant files under `project-canon/`

`project-canon/` is the curated authoritative source of truth.
`docs/` may exist as legacy/reference material only.

Do not modify sibling projects unless explicitly instructed.
